module.exports = {
  gameTypes: Object.freeze({
    NORMAL: 'normal',
    RANKED: 'ranked',
    CUSTOM: 'custom',
  }),
  matchStates: Object.freeze({
    CREATION: 'creation',
    IN_PROGRESS: 'in progress',
    FINISHED: 'finished',
  })
}
